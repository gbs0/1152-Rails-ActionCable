<div id="message-<%= message.id %>">
    <small>
        <strong><%= message.user.username %></strong>
        <i><%= message.created_at.strftime("%a %e %b at %l:%M") %></i>
    </small>
    <p><%= message.content %></p>
</div>